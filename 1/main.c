#include "matrix.h"

int main() {
    // Matrix *matrix = malloc(sizeof(Matrix));

    // input(matrix);
    // output(*matrix);
    // erase(matrix);

    // free(matrix);

    ind_task();
    return 0;
}